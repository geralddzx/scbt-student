<% if (Scbt.Models.user.permissionType() == "admin"){ %>
  <a href="#courses/new" class="btn btn-default">Create New Course</a>
<% } %>

<br>

<h3>All Courses</h3>
<ul>
  <% courses.each(function(course){ %>
    <li>
      <a href="#courses/<%= course.id %>">
        <%= course.escape("name") %> <%= course.escape("code") %>
      </a>
      
      <% if (courses.url.indexOf("user") != -1){ %>
        Start Date<%= course.escape("start_date") %>
        End Date<%= course.escape("end_date") %>
        Status <%= course.escape("status") %>
        Grade <%= course.escape("grade") %>
      <% } %>
      
      <% if (Scbt.Models.user.permissionType() === "admin"){ %>
        <button class="remove-course" id="<%= course.id %>">Remove Course</button>
        <a href="#courses/<%= course.id %>/edit">Edit Course</a>
      <% } %>
    </li>
  <% }) %>
</ul>



