<h3><%= title %> Course</h3>
<form>
  <label for="name">Name
    <input type="text" name="course[name]" 
      value="<%= course.escape("name") %>" id="name">
  </label>
  
  <label for="code">Code
    <input type="text" name="course[code]"
      value="<%= course.escape("code") %>" id="code">
  </label>
  
  <label for="start-date">Start Date
    <input type="date" name="course[start_date]"
      value="<%= course.escape("start_date") %>" id="start-date">
  </label>
  
  <label for="end-date">End Date
    <input type="date" name="course[end_date]" 
      value="<%= course.escape("end_date") %>" id="end-date">
  </label>
  <label for="hours">Hours
    <input type="number" name="course[hours]"
      value="<%= course.escape("hours") %>" id="hours">
  </label>
  
  <% if (Scbt.Models.user.get("permission") == "MASTER_ADMIN"){ %>
    <label>Select an Instructor
      <select name="course[instructor_id]">
          <option value="" 
          <% if (!course.instructor){ %>
            selected="selected"
          <% } %>
          >No Instructor</option>
        <% course.instructors.each(function(instructor){ %>
          <option value="<%= instructor.get("id") %>" 
            <% if (course.instructor && course.instructor.get("id") == instructor.get("id")) { %>
              selected="selected"
            <% } %>
          >
            <%= instructor.get("first_name") %> <%= instructor.get("last_name") %>
          </option>
        <% }) %>
      </select>
    </label>
  <% } %>

  <input type="submit" value="<%= action %> Course">
</form>